export enum EventTypeId {
    pla = 'Pla',
    enf = 'Enf',
    amb = 'Amb',
    aula = 'Aula',
    posp = 'PosP',
}

export enum EventTypeDesc {
    pla = 'Plantão',
    enf = 'Enfermaria',
    amb = 'Ambulatório',
    aula = 'Aula',
    posp = 'Pós Plantão',
}

export enum EventWatchId {
    manha = 'manha',
    tarde = 'tarde',
    noite = 'noite'
}

export enum EventWatchDesc {
    manha = 'Manhã',
    tarde = 'Tarde',
    noite = 'Noite'
}

export enum CalendarViewType {
    month = 'month'
}

export enum LocalStorageKeys {
    events = 'events',
    notes = 'notes'
}

export enum MomentGranularity {
    day = 'day'
}